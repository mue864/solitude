/**
 * Base URL for the Solitude backend API.
 *
 * - Android emulator  → 10.0.2.2 (loopback alias)
 * - iOS simulator     → localhost
 * - Physical device   → your machine's LAN IP (e.g. 192.168.x.x)
 *
 * Change this to your LAN IP when testing on a physical device.
 */
export const API_BASE_URL = "http://10.42.0.1:8080";
